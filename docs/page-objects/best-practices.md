
- Keep page objects small and focused
- Compose shared regions into reusable components
- Use factory elements for dynamic or library-specific widgets
- Use descriptive property names
- Don't expose internal implementation details — treat page objects as a public API
